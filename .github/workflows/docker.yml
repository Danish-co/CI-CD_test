name: Docker CI

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build frontend image
        run: |
          docker build \
            --build-arg API_BASE_URL=http://localhost:9000 \
            -t ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/ci-cd-test-frontend:${{ github.sha }} \
            .

      - name: Push frontend image
        run: |
          docker push \
            ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/ci-cd-test-frontend:${{ github.sha }}

      - name: Build API image
        run: |
          docker build \
            -t ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/ci-cd-test-api:${{ github.sha }} \
            ./api

      - name: Push API image
        run: |
          docker push \
            ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/ci-cd-test-api:${{ github.sha }}